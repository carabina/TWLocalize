platform :ios, ‘9.3’

target 'TWLocalize' do
  use_frameworks!
  pod 'TWLocalize', :git => 'https://github.com/tom-wolters/TWLocalize.git'

  target 'TWLocalizeTests' do
    inherit! :search_paths
    pod 'TWLocalize', :git => 'https://github.com/tom-wolters/TWLocalize.git'
  end

end
